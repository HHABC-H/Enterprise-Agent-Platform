global:
  scrape_interval: 10s
rule_files:
  - /etc/prometheus/alerts.yml
scrape_configs:
  - job_name: enterprise-agent-platform
    metrics_path: /actuator/prometheus
    static_configs:
      - targets: ["app:8080"]

# 前后端联调记录模板

> 使用日期：____年__月__日  
> 测试人员：____________  
> Git 提交版本：____________  
> Java 版本：____________  
> 浏览器：____________

## 1. 环境记录

| 项目 | 实际值 | 结果 |
| --- | --- | --- |
| 后端地址 | `http://localhost:8080` | □ |
| 前端地址 | `http://localhost:5173` | □ |
| Spring profile | `local` / `docker` | □ |
| `/actuator/health` | `UP` / 其他 | □ |
| 前端首页 | HTTP 200 / 其他 | □ |

## 2. 页面操作记录

| 编号 | 页面区域 | 操作 | 预期结果 | 实际结果 | 截图/Trace ID |
| --- | --- | --- | --- | --- | --- |
| 1 | 知识入库 | 提交公开 Markdown | 返回文档 ID、Chunk 数量 |  |  |
| 2 | 入库状态 | 点击查询入库状态 | 状态为 `SUCCESS` |  |  |
| 3 | 证据问答 | 提问文档中存在的问题 | 返回答案、证据和轨迹 |  |  |
| 4 | 证据问答 | 提问文档外的问题 | 明确拒答 |  |  |
| 5 | 关系图谱 | 查询同租户同版本文档 | 展示关系或空结果说明 |  |  |
| 6 | 人工审批 | 开启审批并提交通过 | 先等待，后完成 |  |  |
| 7 | 评测中心 | 运行内置评测 | 返回评测结果 JSON |  |  |

## 3. API 记录

| 接口 | 方法 | HTTP 状态 | traceId | 结论 |
| --- | --- | --- | --- | --- |
| `/api/documents/markdown` | POST |  |  |  |
| `/api/documents/{documentId}/ingestion-status` | GET |  |  |  |
| `/api/chat` | POST |  |  |  |
| `/api/graph/relations` | GET |  |  |  |
| `/api/workflows/{workflowId}/approval` | POST |  |  |  |
| `/api/evaluations/run` | POST |  |  |  |

## 4. 安全与异常记录

- [ ] 未授权用户无法检索专属文档。
- [ ] 不同租户之间不会互相召回文档。
- [ ] 证据不足时不生成猜测性答案。
- [ ] 空审批意见被拒绝。
- [ ] 非法审批人被拒绝。
- [ ] 重复提交同一内容不会重复建立索引。
- [ ] 响应中没有出现密码、模型密钥或完整敏感对话。

## 5. 最终结论

- [ ] 通过：local profile 可演示闭环。
- [ ] 有条件通过：核心链路通过，外部服务待联调。
- [ ] 不通过：存在阻断问题。

阻断问题或遗留项：

```text
请在此记录错误现象、接口、Trace ID、日志时间和复现步骤。
```

